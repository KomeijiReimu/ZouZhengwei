<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rentcar.mapper.VehicleMapper">

    <update id="modifyVehicle">
        update vehicle
        set brand = #{brand},
            kind  = #{kind},
            model = #{model},
            price = #{price}
        where id = #{id};
    </update>
</mapper>